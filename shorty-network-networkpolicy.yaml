apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: shorty-network
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/shorty-network: "true"
  podSelector:
    matchLabels:
      io.kompose.network/shorty-network: "true"
